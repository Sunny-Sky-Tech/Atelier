//Example Config

const host = INSERT_HOST
const user = INSERT_USER
const password = INSERT_PASSWORD
const database = INSERT_DATABASE


module.exports = {
  host: host,
  user: user,
  password: password,
  database: database,
}